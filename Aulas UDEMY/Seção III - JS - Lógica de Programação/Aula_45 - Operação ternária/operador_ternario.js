/*

Operação Ternária - Substitui o uso de um if e else.

Sintáxe:

const valorX = 1000;               if              else
const valorMaximo = valorX >= 1000 ? 'Valor Máximo' : 'Valor Inferior';

Uso do || (ou):

const corUsuario = 'null';
const corPadrao = corUsuario || 'Preta';

*/